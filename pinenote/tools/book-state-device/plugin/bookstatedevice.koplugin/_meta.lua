local _ = require("gettext")
return {
    name = "bookstatedevice",
    fullname = _("Book State (experimental)"),
    description = _([[A fixed, opt-in persistent note whose application runs inside gVisor.]]),
}
