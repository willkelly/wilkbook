local _ = require("gettext")

return {
    name = "bookstatedevicerealui",
    fullname = _("Book State device real-UI probe (fixture)"),
    description = _([[Host-only controller for the production plugin's native KOReader widget test.]]),
}
