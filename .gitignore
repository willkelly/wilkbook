*~
.sisyphus/

# licensed fonts staged locally for image builds; never committed
/pinenote/fonts/local/
.claude/worktrees/

# generated Python bytecode
__pycache__/
*.py[cod]

# power evidence may contain MAC addresses, command lines, and mount details
/pinenote-power-*.scm
/power-*-before.scm
/power-*-after.scm
/power-*-delta.scm

# per-checkout build flags (e.g. WILKBOOK_VERY_INSECURE_FOR_CONVENIENCE);
# never committed -- see pinenote/insecure.scm
/local.mk

# per-device PineNote backups contain irreplaceable factory calibration
# and must never be committed
/device-backups/
